
#Область ИнтерфейсАвтоматическихТестов

Перем ЮнитТест;

Функция ПолучитьСписокТестов(ЮнитТестирование) Экспорт
	
	ЮнитТест = ЮнитТестирование;
	
	СписокТестов = Новый Массив;
		
	Возврат СписокТестов;
	
КонецФункции
 
#КонецОбласти

#Область ПрограммныйИнтерфейс

Функция ПолучитьПеречислениеВидовКолонок() Экспорт

	МассивПеречислений = Новый Массив;
	
	МассивПеречислений.Добавить("Реквизит объекта метаданных");
	МассивПеречислений.Добавить("Дополнительный реквизит");
	МассивПеречислений.Добавить("Дополнительное свойство");
	МассивПеречислений.Добавить("Цена номенклатуры");
	МассивПеречислений.Добавить("Цена поставщика");
	
	Возврат МассивПеречислений;

КонецФункции // ПолучитьПеречислениеВидовКолонок()

Функция ПолучитьМассивКолонок() Экспорт

	МассивКолонок = Новый Массив;
	
	СтруктураКолонки = Новый Структура;
	СтруктураКолонки.Вставить("Имя", "Артикул");
	СтруктураКолонки.Вставить("Тип", Новый ОписаниеТипов("Строка",,,, Новый КвалификаторыСтроки(35)));
	СтруктураКолонки.Вставить("Заголовок", "Артикул");
	
	МассивКолонок.Добавить(СтруктураКолонки);
	
	СтруктураКолонки = Новый Структура;
	СтруктураКолонки.Вставить("Имя", "Код");
	СтруктураКолонки.Вставить("Тип", Новый ОписаниеТипов("Строка",,,, Новый КвалификаторыСтроки(11)));
	СтруктураКолонки.Вставить("Заголовок", "Код");
	
	МассивКолонок.Добавить(СтруктураКолонки);
	
	Возврат МассивКолонок;

КонецФункции // ПолучитьСписокКолонок()

#КонецОбласти