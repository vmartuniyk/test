

&НаСервере
Процедура ВыполнитьИмпортНаСервере(Адрес)
	РеквизитФормыВЗначение("Объект").ВыполнитьИмпортХарактеристик(Адрес);
КонецПроцедуры


&НаКлиенте
Процедура ВыполнитьИмпорт(Команда)
	
	Перем Адрес;
	
	Если ПоместитьФайл(Адрес, , , Истина) Тогда
		ВыполнитьИмпортНаСервере(Адрес);
	КонецЕсли;
	
КонецПроцедуры
 